# THINKING：这道题是什么，以及我是怎么一步步想的

> 这是 [README.md](README.md) 的姊妹篇。README 记录"最终做成了什么"，这份文档记录
> "当时的思路是怎么演进的"——包括走对的弯路、被数据推翻的假设、以及每个 bug 是怎么
> 被发现和定位的。

## 一、问题是什么

给定一个无向图的 CSV（每个节点一行：`node_id, initial_label, "[邻居列表]"`，邻居
**包含自己**），实现**同步标签传播**：每一轮，所有节点同时根据**上一轮**的邻居标签
统计最高频标签作为自己的新标签，平票取字典序最小者；直到某轮无任何变化。输出收敛
后的 `node_id,final_label`，按 node_id 升序。

真正的难点藏在这句不起眼的话里：**"端到端计时，按运行时间排名"**。也就是说：

1. 读文件、解析、迭代、排序、写文件，**全部**计入成绩；
2. 算法本身（标签传播）不是难点——它每轮只是对邻居做多 数投票；
3. 这道题本质是一道 **I/O 与内存访问模式的工程优化题**，胜负在解析器和缓存行为里。

我的测试基准是一份 200 万节点、885MB、1.19 亿边端点的图。全部优化都是围绕它测量的。

## 二、开局：先想清楚三件事

**第一，目标环境是什么。** 题面说 g++ 15.2 + CMake、`./label_propagation` 的调用
方式——这是 Linux 惯例。所以开发测试全程在 WSL Ubuntu 里做，避免"Windows 上能跑、
评测机上翻车"。也特意不在 `/mnt/c`（Windows 挂载）下编译——那是 9p 协议的慢速文件
系统，测出来的数字毫无意义。

**第二，零依赖。** 标准库 + OpenMP 完全够用，引入第三方库只会增加评测机构建环境
的不确定性。

**第三，正确性怎么保证。** 在写第一行优化代码之前，先写了一个**逐字照规格实现的
Python 参考实现**（`bench/verify.py`），再加一个数据生成器。此后每一版优化都要先
过这一关才谈性能。事后证明这救了我至少三次。

## 三、v1：先做对，再测量

第一版故意写得朴素：整体读入、单线程解析、哈希表做 ID 映射、OpenMP 并行迭代、
拼接输出。**18.1 秒**。

分段计时一出来，结论毫无悬念：

```
read   0.98s   5%
passA  0.91s   5%    （行 ID / 标签解析）
passB 15.56s  84%    ← 1.19 亿次邻居 ID 哈希查找
iterate 0.43s  2%
```

passB 慢在哪？每次查找 = 算哈希 + 在一张 2M 条目（64MB）的开寻址表上**随机访问**
——几乎每次都是缓存不命中，~130ns × 1.19 亿次 ≈ 15 秒。这就是主战场。

**这里踩了第一个坑**（也是为什么坚持交叉验证）：官方 4 节点样例通过了，但 5 组
随机图里有一组稀疏图失败——分析发现我解析 `"[1,2,3]"` 时，最后一个 ID 把 `]` 一起
吃了进去（扫描只停在 `,`，而 `]` 不是逗号），于是每行最后一个邻居都变成了带 `]`
的"幽灵节点"。幽灵节点没有初始标签，默认投了字典序最小的那一票，稀疏图上这一票
足以改变传播结果。**官方样例通过纯属巧合**（幽灵票恰好也投 black）。教训：样例太
小，必须用随机数据 + 参考实现交叉验证。

## 四、v2：并行化，但怎么并行才是问题

"把 passB 并行化"说起来简单，直接做是错的——多个线程往一张共享哈希表里插，要么
加锁慢死，要么无锁算法复杂到没法验证。

我的思路是**让随机访问变小**：每个线程解析自己那段行区间，用**线程私有的本地
interner**（表小得多、且天然无竞争），解析完再把各自的去重结果**合并**到一个全局
ID 空间。合并用 256 个分片表 + 分片互斥锁——不同分片互不干扰，锁竞争被稀释到可以
忽略。

另一个决定：**用 mmap 替代显式读文件**。这样"读入"不再是独立阶段，页错误会和
并行解析自然重叠，冷缓存时读 I/O 被计算时间吸收掉。

**18.1s → 4.1s**。这轮还埋了一个雷（后面爆）：v2 的 rowOff 少了末尾哨兵，最后一
行的度数读到越界垃圾值——小样例表现为"邻接表为空"，大数据直接段错误。修一行搞定，
但过程提醒我：**内存越界类 bug 在小数据上可能是静默的**，ASan 应该常备。

## 五、v3：一次失败的尝试（SoA）与它的教训

下一个瓶颈是"行扫描"（串行 memchr，0.8s）和输出排序（0.4s）。这些都好办：并行
扫描、基数排序、并行拼装输出。

但我同时做了一个"教科书式"的优化——把 interner 改成 SoA（探测槽位数组与键数据分
离，槽位从 32B 降到 8B）——**结果反而变慢了**。原因值得记住：SoA 把"命中"路径从
一次随机访问变成两次（先读槽位拿到 id，再读键数组做 memcmp），而邻居查找 87% 是
命中。缓存友好性要按**实际访存路径**算，不能只看单个结构体的字节数。回退到 AoS。

## 六、v4：换个 ID 语义，消灭一整类工作

这时我重新审视一个习以为常的设计：为什么全局节点 ID 要由哈希表"发现顺序"决定？
**行号本身就是天然的 ID**——每个节点恰好一行。于是：

- 第一遍并行把所有行 ID 插入分片表（值 = 行号）；
- 第二遍把邻居 ID 解析成行号；
- 关键收益：邻接表从"随机 scatter 填充"变成**按行序的纯顺序拷贝**——每个线程把自己
  那段邻居数组 memcpy 式地拷进去，随机写消失。

配套的小优化：`make_unique_for_overwrite` 免掉 476MB 邻接数组的零初始化。

## 七、v5：为数字定制一把钥匙

再往后，profile 显示解析里剩下的成本全是哈希探测。这时注意到一个数据事实：**node
ID 几乎总是纯数字**。那就可以不存字符串——用 `(数值, 长度)` 二元组做键：

- 它是**单射**的：两个不同的纯数字串不可能有相同的（值，长度）——"10" 和 "010"
  值同长异，正是不同的 ID；
- 于是探测槽位从 32B 降到 16B，哈希是两次乘法，比较是两个整数相等——**没有
  memcmp，没有碰撞**。

前导零等"非规范"形态自动落入原来的字符串路径，正确性不受影响。**1.6s**。

## 八、v6-v7：和内存延迟玩心理战

到这里，每个邻居 ID 的处理成本就是"一次预取不到的随机访存"。对付它只有一个办法：
**流水线预取**——解析第 i+3 个邻居 ID 时，顺手把它的探测槽位地址预取了；等轮到它
被 intern 时，数据已经在 L1 里。环形队列深度从 1 级加到 4 级，配合按 6 倍行数
预分配哈希表（避免 rehash 时的整表重涂），解析 0.75s → 0.39s。

这期间有个插曲值得记录：一个只在 `-O3` 下稳定崩溃、`-O1` 和 ASan 下都好好的段错
误。最后 ASan 配 `-O3` 复现出真相——`intern()` 里 `return e.id` 这行在 `grow()`
释放旧表之后还通过悬空引用读值。**编译器优化等级改变了 UB 的可见性**，而"ASan 没
报"不等于"没有 UB"。

## 九、v8：把"查表"变成"看一眼"

最大的单步突破来自一个数学观察。预扫描阶段我已经知道每行的 ID 字符串；如果第 v
行的 ID **恰好是 v 的规范十进制形式**（纯数字、无前导零、值等于行号——即
"0","1","2",…"2000000" 这种最常见形态），那么：

> 邻居 ID 解析出数值 val 后，只要 `val < 行数` 且 **`rowCanon[val] == 该 ID 的位数`**
> （一个预取好的字节），它就必然指向第 val 行。

为什么单字节比较就够？因为"纯数字 + 无前导零 + 数值等于 val + 长度等于 val 的最
小位数"**逻辑上等价于**"这个字符串就是 val 的十进制表示"。没有哈希、没有探测、
没有 memcmp——稠密数值文件下 ~100% 命中，整个 fallback interner 几乎空转。

配套地把"行 ID 插入"融进解析 region（与慢线程的解析重叠）、把"resolve"和"CSR
拷贝"融合成一个 region（各线程只依赖自己的映射，天然无交叉）。**0.96s**。

这个设计也差点被一个 bug 毁掉：输出阶段的偏移前缀按**文件序**累加，写入却按
**排序后位置**。数值 ID 排序后恰好是恒等排列，把这个 bug 藏得严严实实；直到我用
字符串 ID 数据测试，输出立刻互相覆盖。又一次验证：**测试数据的形态必须刻意覆盖
"让假设失效"的分支**。

## 十、v9 与终点：知道何时停

最后收掉零碎的浪费（输出缓冲免零初始化等）后是 **0.91s**。此时分段计时是：

```
prepass      0.06s   （数行 + 提取行 ID，含冷页首次触碰）
parse        0.24s   （全文件必须被触碰一遍 + 每邻居一个预取好的字节）
resolve+csr  0.05s
iterate      0.47s   （两轮强制全量 = 内存带宽墙）
write        0.08s
```

我试着继续压迭代阶段：预取距离 4/12/24、静态/动态调度、大页 madvise——**全部在
噪声以内**。这说明剩下的成本是物理的：1.19 亿条边 × 2 轮的访存已经顶到这台虚拟机
的内存带宽。此时的正确决策是**停下来**，而不是继续用噪声当收益自我安慰。

顺带一提，迭代阶段我提前埋了一个保险：**活跃前沿模式**——当变更数降到 N/4 以下，
后续轮次只重算"邻居上轮变过的节点"。我的测试数据 2 轮就收敛体现不出价值，但如果
评测数据需要几十轮，它能防止运行时间随轮数线性爆炸。压测时还发现纯网格图在同步
更新下会周期 2 振荡（棋盘构型每轮全体翻转）——这不是我的 bug，参考实现同样永不
终止；规格保证评测数据收敛，我把这一点如实写进了 README 的"已知边界"。

## 十一、方法论复盘

1. **先建裁判，再上赛场**。Python 参考实现 + 随机数据生成器在写优化代码之前就位，
   每版先过正确性。它抓住了 `]` 幽灵节点、输出错位、哈希污染三个我自查绝无可能
   发现的问题。
2. **测量驱动，一次一个假设**。每轮优化前先分段计时，找到占比最大的那一项，只对
   它动刀，立刻验证。18.1 → 4.1 → 2.9 → 2.7 → 1.6 → 0.96 → 0.91，每一步都有
   明确的"当时瓶颈"。
3. **优化要顺应数据形态**。数值 ID、行序即节点序、社区局部性——这些"数据的性质"
   比任何通用技巧值钱。规范十进制快路径本质上是把"数据是什么样"编译进了一个字节
   的判断里。
4. **警惕被优化等级和巧合掩盖的 bug**。UAF 在 -O1 下沉默；输出错位在恒等排列下
   隐形；官方样例纵容了幽灵节点。覆盖"反面形态"的测试（字符串 ID、乱序、-O3、
   ASan+O3）才是真测试。
5. **失败要记录**。SoA 教会我访存路径要整体算，写进文档后我再也没有犯过。

## 十二、成果

| 指标 | v1 | v9 |
|---|---|---|
| 2M 节点 / 885MB 端到端（14 线程） | 18.1s | **0.91s（~20×）** |
| 8 线程（评测机同构） | — | 1.13-1.43s |
| 5M 节点 / 2.3GB（8 线程） | — | 5-6s |
| 峰值内存 | — | 2.3GB |

无第三方依赖，C++20 标准库 + OpenMP；`-Wall -Wextra -Wpedantic` 零警告；
ASan/UBSan 干净。
